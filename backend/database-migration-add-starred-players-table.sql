CREATE TABLE IF NOT EXISTS starred_players (
  id SERIAL PRIMARY KEY,
  scout_id VARCHAR(255) NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  athlete_id VARCHAR(255) NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMP DEFAULT NOW(),
  UNIQUE (scout_id, athlete_id)
); 