{
  "buildSettings": {
    "ENABLE_BITCODE": "NO",
    "IPHONEOS_DEPLOYMENT_TARGET": "14.0",
    "SWIFT_VERSION": "5.0"
  },
  "preBuildScripts": [
    {
      "name": "Install Dependencies",
      "script": "cd $CI_WORKSPACE/frontend && npm ci && npx cap sync ios && cd ios/App && pod install --repo-update"
    }
  ]
}
